# Funtion in Java
# Type of Functions
1. In-built Functions: nextInt(), length()
2. User-Defined:
    -Function with return and without parameter
    -Function with paerameter and without return
    -Function without return and without parameter
    -Function with return and with parameter
# Recursion
- A function calls itself
- It uses base case and recursive case
> Type of recursion
   1. Direct Recursion[a function calling itself directly]
   2. Indirect Recursion[a->b->c]
   3. Tree Recursion
   4. Nested Recursion: McCarthy91
   5. Head Recursion
   6. Tail Recursion 

# Call Stack
- Used for storing calls of function and returns the value
- call stack is a memory frame
- push() and pop()

# OOPs Concept
- It is the License to DSA

>Class
- Class is the blueprint of data,data Structures and Functions
- It is the In-built data type
- We can create the class inside or outside the main class
- outside we can use final,abstract,default
- Inside we can use public etc 
> Objects
- Real world Entity[data,data structures and functions]
- object are also instance
>ways of writting a class and OOPs Intro

> Structure
- It is also a data type

# Variable
A container which can store a value
> Types of Variable
- class variable [can be accessed inside the class]
- instance variable [can be accessed inside and outside the class]
- loacl variable [access inside the method of a class]
- Static variable[dont need to create the instance of a variable]
>Instance
- storing and accessing the objects

# Encapsulatrion
- Wrapps data and functions
- Example is class
- getters(getting the private variable)and setters()

# Polymorphism
1. Runtime polymorphism[]
2. Compiletime Polymorphism[method overloading and operator overloading(op ovrld is not possible in java)]
    - method overloading means same name different characteristrics

# Inheritance
> types
- Single
- Multilevel
- hiearcial
- hybrid


1. Attribute:int data=10
2. Object/Instance:
3. Variables:

 # Constructors
 [Intitialize class,construct a class,create ur own data type, no return value, has access specifiers]
 1. Default
 2. Parametric
 3. Non parametric
 4. Copy Constructor
 5. Private
 >constructor overloading